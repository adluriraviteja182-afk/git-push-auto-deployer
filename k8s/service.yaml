apiVersion: v1
kind: Service
metadata:
  name: git-auto-deployer-svc
  namespace: production
spec:
  selector:
    app: git-auto-deployer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP